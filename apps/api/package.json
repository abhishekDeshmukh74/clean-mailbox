{
  "name": "api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uv run uvicorn clean_mailbox_api.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "uv sync",
    "lint": "uv run ruff check .",
    "typecheck": "echo 'no typecheck configured'"
  }
}
